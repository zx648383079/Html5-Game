enum Status {
    NONE     = 0,
    SELECTED = 1,
    USED     = 2	
}

enum Game {
    NONE     = 0,
    SURROUND = 1,
    END      = 2	
}

enum Kind {
    CIRCLE = 0,
    CAT    = 1	
}

enum Colors {
    '#999' = 0,
    '#f00' = 1,
    '#fff' = 2
}
enum Direction {
    LEFT        = 0,
    LEFTTOP     = 1,
    RIGHTTOP    = 2,
    RIGHT       = 3,
    RIGHTBOTTOM = 4,
    LEFTBOTTOM  = 5
}

const PLAY_IMG = 'play';
const CAT_IMG = 'cat';
const CATED_IMG = 'cated';
